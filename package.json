{
  "name": "contextlint",
  "version": "0.4.1",
  "description": "Keeps a project's always-on Claude instructions from growing without bound",
  "type": "module",
  "bin": {
    "contextlint": "bin/contextlint.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.3"
  }
}
